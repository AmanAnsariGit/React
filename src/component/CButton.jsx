function CButton(props) {
    const {title} = props
    return(
        <>
            <button onClick={() => console.log(message)};
            "Hello"}>
                {title}
            </button>
        </>
    )
}

export default CButton;